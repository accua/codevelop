<% if authenticated? %>
  <%= erb :sign_up_github, :locals => {:errors => session[:error]} %>
<% else %>
  <%= erb :sign_up_default, :locals => {:client_id => CLIENT_ID, :errors => session[:error]} %>
<% end %>
